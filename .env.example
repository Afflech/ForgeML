# Kaggle authentication is handled by the Kaggle CLI.
# Run 'kaggle auth login' to store credentials in ~/.kaggle/access_token.

# LLM planner (Phase 4 only)
# OPENAI_API_KEY=

# Optional overrides
# FORGEML_ARTIFACTS_DIR=
